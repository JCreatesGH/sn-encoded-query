{
  "name": "sn-encoded-query",
  "version": "0.2.0",
  "description": "A typed builder, parser, explainer, and validator for ServiceNow encoded queries.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "sn-encoded-query": "dist/cli.js" },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": { "test": "vitest run", "build": "tsc -p tsconfig.json", "prepare": "npm run build" },
  "keywords": ["servicenow", "encoded-query", "gliderecord", "query-builder", "typescript", "cli"],
  "author": "Joshua John <josh202@gmail.com>",
  "license": "MIT",
  "devDependencies": { "vitest": "^4.0.0", "typescript": "^5.4.0", "@types/node": "^22.19.21" }
}
