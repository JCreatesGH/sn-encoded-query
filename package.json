{
  "name": "sn-encoded-query",
  "version": "0.1.0",
  "description": "A typed builder, parser, and validator for ServiceNow encoded queries.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "test": "vitest run", "build": "tsc -p tsconfig.json" },
  "keywords": ["servicenow", "encoded-query", "gliderecord", "query-builder", "typescript"],
  "author": "Joshua John <josh202@gmail.com>",
  "license": "MIT",
  "devDependencies": { "vitest": "^4.0.0", "typescript": "^5.4.0" }
}
